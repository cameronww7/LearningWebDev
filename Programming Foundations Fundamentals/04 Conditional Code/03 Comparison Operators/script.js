var a = 123;
var b = "123";

// equality check
// = will Assign!! Whattt!
// === Strict
// !== not strictly equal
if ( a == b && a === b ) {
   alert("Yes, they ARE equal");
} else {
   alert("No, they're NOT equal");
}